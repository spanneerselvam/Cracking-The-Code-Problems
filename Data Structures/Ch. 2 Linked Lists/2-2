#Implement an algorithm to find the kth to last element of a singly linked list
import Node from linked_list
import LinkedList from linked_list
def find_element(LinkedList, k):
    length = LinkedList.length() - k
    return LinkedList.get(length)

a = LinkedList()
s = "FOLLOW UP"
my_list = []
for ch in s:
    a.append(ch)
print(find_element(a,2))
